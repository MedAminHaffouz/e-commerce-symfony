{% extends 'dashboard/UserDashboard.html.twig' %}

{% block title %}Statistiques Formateur{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">📊 Mes Statistiques</h1>

        <ul class="list-group mb-4">
            <li class="list-group-item">Total Formations créées : <strong>{{ totalFormations }}</strong></li>
            <li class="list-group-item">Formations publiées : <strong>{{ publishedCount }}</strong></li>
            <li class="list-group-item">Total Revenu généré : <strong>{{ totalRevenue }} €</strong></li>
        </ul>

        <h3>Ventes par Mois :</h3>
        <table class="table table-bordered">
            <thead>
            <tr>
                <th>Mois</th>
                <th>Nombre de Ventes</th>
            </tr>
            </thead>
            <tbody>
            {% for mois, count in ventesParMois %}
                <tr>
                    <td>{{ mois }}</td>
                    <td>{{ count }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="2" class="text-muted text-center">Aucune vente enregistrée.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
